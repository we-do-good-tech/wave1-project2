import { LimitInputValDirective } from './limit-input-val.directive';

describe('LimitInputValDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitInputValDirective();
    expect(directive).toBeTruthy();
  });
});
